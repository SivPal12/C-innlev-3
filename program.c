#include <stdio.h>
#include <stdlib.h>

const int with = 10, height = 20;
int canvas[with][height];

int main (int argc, char *argv[]) {
  // Setup screen
  while (true) {
    // ReadUserInput
    // UpdateGameState
    // RenderGame
  }
}
